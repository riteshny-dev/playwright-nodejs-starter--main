import {test, expect} from '@playwright/test'

test('This is the First test',async({})=>{

})

test('This is the Second test', async ({ page }) => {
    
})

// test('This is the Second test', async ({ page }) => {
    
// }) It will not allowed for the same test name

test('This is the Third test', async ({ page }) => {
    
})


